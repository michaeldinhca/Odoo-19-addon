<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_calendar_event_form_inherit_ngyn_gantt" model="ir.ui.view">
        <field name="name">calendar.event.form.inherit.ngyn.gantt</field>
        <field name="model">calendar.event</field>
        <field name="inherit_id" ref="calendar.view_calendar_event_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_ids']" position="after">
                <div class="mt-2">
                    <label for="x_ngyn_installer_ids" string="Installers" class="o_form_label"/>
                    <field name="x_ngyn_installer_ids" widget="many2many_tags"
                           placeholder="Assign installer(s)..."
                           class="oe_inline"/>
                </div>
            </xpath>
        </field>
    </record>
</odoo>
